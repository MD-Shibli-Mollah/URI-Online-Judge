#include <stdio.h>

int main()
{
    int c,sum=0;
    char a,b = 'C';
    scanf("%d %c",&c, &a);
   // a = getchar();
    if(a == b)
    {
        sum = sum +c;
        printf("dooo %d",sum);
    }
    return 0;
}
